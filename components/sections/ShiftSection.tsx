/**
 * @deprecated Replaced by `SolutionSection`. Kept only as a thin re-export so
 * any stale imports keep working. Safe to delete.
 */
export { SolutionSection as ShiftSection } from "./SolutionSection";
