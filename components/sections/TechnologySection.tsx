/**
 * @deprecated Split into `GaussianSplattingSection` and `DigitalTwinSection`.
 * Kept only as a thin re-export so any stale imports keep working. Safe to delete.
 */
export { GaussianSplattingSection as TechnologySection } from "./GaussianSplattingSection";
